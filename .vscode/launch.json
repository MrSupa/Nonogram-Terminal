{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Nonogram",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/nonogram.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Make Build",
            "terminal": "integrated"
        }
    ]
}
