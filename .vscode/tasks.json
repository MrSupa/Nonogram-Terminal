{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make Build",
            "type": "shell",
            "command": "D:\\msys64\\usr\\bin\\bash.exe",
            "args": [
                "-c",
                "pwd",//This just makes it work!
                "cd \"${workspaceFolderForwardSlash}\" && mingw32-make all"
            ],
            "options": {
                "env": {
                    "MSYSTEM": "CLANG64",
                    "PATH": "/clang64/bin:/usr/bin:${env:PATH}"
                }
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
